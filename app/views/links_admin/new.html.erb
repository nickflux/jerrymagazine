<h2>New link</h2>

<%= form_for :link, @link, :url => {:action => 'create'} do |f| %>
 <%= render :partial => "form", :locals => {:f => f} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', links_path, :class => "button" %>