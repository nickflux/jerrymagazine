<p>
  <b>Title:</b>
  <%=h @edition.title %>
</p>

<p>
  <b>Edition no:</b>
  <%=h @edition.edition_no %>
</p>

<p>
  <b>Publish date:</b>
  <%=h @edition.publish_date %>
</p>

<p>
  <b>Description:</b>
  <%=h @edition.description %>
</p>

<%= link_to 'Back', editions_path %>

<h2>Edition works</h2>

<table>
  <tr>
    <th>Type</th>
    <th>Title</th>
    <th>Abstract</th>
    <th>Work content</th>
    <th>User</th>
  </tr>

<% @edition.works.each do |work| %>
  <tr>
    <td><%=h work.work_type %></td>
    <td><%=h work.title %></td>
    <td><%=truncate(h(work.abstract)) %></td>
    <td><%=truncate(h(work.work_content)) %></td>
    <td><%=h work.user.get_fullname %></td>
    <td><%= link_to 'Show', work %></td>
  </tr>
<% end %>
</table>